# V28

- Hero `I DESIGN` / `WORLDS` left edge aligned while keeping the group centered.
- ABOUT rebuilt as identity → working method → credentials → project timeline; profile image cropped before the awkward lower shoulder/torso area.
- CCIT removed from timeline and labels.
- Portfolio cards use the 1.586:1 credit-card ratio, full-bleed imagery, and only project title + genre. Desktop shows five cards at once; the sixth is reached by horizontal scroll.
- Added Kinosis as project 04; De.Co and Machinator shift to 05/06. Fernand/Kinosis use temporary product-UI covers rather than unrelated art.
- Journal is now a blog: latest three on home, full archive, individual post pages, and owner CRUD. Owner writes require GitHub repository write permission; no secret is embedded in the site.
- Project transition is image-only. Text appears after the separate detail page loads.
- Detail headers use text-only `KWAK TAEWOONG`; the old circular mark is removed and only the brand text is clickable.
- BACK always returns to `../index.html#portfolio`; history-based re-entry loop removed.
- Korean typography uses a native Korean sans stack to avoid the previous mismatched feel.
