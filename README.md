# KWAK TAEWOONG — Personal Website v33

`index.html` is the main page.

## v33 highlights

- Deterministic project transition: card image → fullscreen raw image → detail hero.
- Reverse transition: project BACK restores the source page and shrinks the same image back into its card.
- BFCache/native-back safety cleanup and transition timeout fail-safes.
- Kinosis cover rebuilt from the supplied app icon; Fernand cover rebuilt from `theme-lunar.jpg` + thin FERNAND wordmark.
- Naver Blog RSS sync every hour at `:17`.
- Journal hierarchy: DEVLOG is primary, CRITIQUE is secondary, LIFE stays on the full blog/index rather than the portfolio home.
- 30-day repository keepalive commit to reduce the chance GitHub disables scheduled workflows after long inactivity.
- Admin content remains separated under `user-content/`; patch ZIPs do not include that folder.

See `ADMIN_GUIDE.md` for update/publish rules.
